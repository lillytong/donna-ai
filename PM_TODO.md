# PM To-Do — donna.ai

## SPEC Health Tracker

Edits since last health check: 3 (F32 firm-profile row, retrieval tier-9, §8 DD-range → DD-80; DD-79+DD-80 in DESIGN_DECISIONS)
Latest check: 2026-06-25 (15th — F03c Mode-B abstain match-confirm decision, DD-78: SPEC §8 range, §11 Mode B steps 3/4/5/6/8/9, §5 F03c row; DD-78 index row + full record). **Hygiene: clean + privacy repair** — DD-78 refs resolve (DD-64/26/27/30/34, F03c, §11 6a/6b/step-4); §8 range corrected DD-74→DD-78 (was stale, pre-existing drift); closed a real §11 gap (new/deleted nodes were *identified* in step 3 but had no specified review path — now staged + reviewed in the content stream). **Privacy: fixed 3 self-introduced + 5 pre-existing persona-name leaks** (`Kevin`→engineering, `Katrina`→product across F01c/F10b/F27/DD-75-rationale + my F03c/§11/DD-78 edits) — `grep Kevin|Katrina` now 0 across both repo docs (was a live public-repo violation). **Structural: skeleton dumped (§1–17, unchanged) + §11 + §5 read end-to-end** — match-confirm slotted as a sub-step of the existing step-6 structural phase (no new top-level step, no reorder); the four buckets fold into existing steps 3/5/8/9 rather than a parallel section; DD-78 appended at the DESIGN_DECISIONS tail in number order, index row in sequence. §9 remains THE standing split candidate (propose-first, awaiting Lilly's OK since 2026-06-23) — not re-litigated. Both verdicts present → counter reset to 0.
Prior: 2026-06-25 (14th — deployment re-scope + a merge-collision repair: DD-74 recorded (v1 **local-only, permanently** — no remote/hosted; runs on either machine, each a standalone install with its **own local Postgres**, **not synced**, divergence accepted; replaces §4's Phase-0-demo→Phase-1-Azure model; hosted phase + Switzerland data-sovereignty deferred to a possible future multi-user/portal, DD-53). SPEC §4 deployment block rewritten; §8 range→DD-74; DD-74 index row; DD-53 carries a forward-note (Azure Phase-1 deferred → auth/identity stay out of v1). Resolved+deleted the "Deployment model re-scope (§4)" open item. **Structural repair:** Kevin's parallel edit to DD-72 (the Note-on-DD-71 + Rationale paragraphs) had been stranded **below** DD-73 by a concurrent-write collision — relocated them back into DD-72 so the record reads in order; verified no content lost (the two paragraphs now appear once, inside DD-72). **Hygiene: clean** — DD-74 refs resolve (§4, §16 out-of-scope hosted-deployment, DD-53, F22, Anthropic DPA); no stale "Phase 1 Azure" left authoritative (§4 + DD-53 both reconciled); §16 already lists hosted deployment out-of-scope (consistent, no edit needed); privacy clean (product owner / operator, no names, no firm). **Structural: skeleton dumped (§1–17, unchanged) + touched sections read end-to-end** — DD-74 appended at the true DESIGN_DECISIONS tail (after the DD-72 repair) in number order, index row in sequence; §4 edit is a like-for-like block swap, no reorder; the DD-72/DD-73 ordering is now correct. §9 remains THE standing split candidate (propose-first, awaiting Lilly's OK since 2026-06-23). Both verdicts present → counter reset to 0. **Coordination note for next session:** Kevin is editing DESIGN_DECISIONS/DEV_TODO concurrently — watch for collisions; he authored DD-72.
Prior: 2026-06-25 (13th — Brainstorm ephemeral-plus-summary reversal: DD-73 recorded (raw Brainstorm stays ephemeral per DD-42; Donna distills one readable per-issue **summary** on close, stored on the issue — distinct from DD-55's cross-deal patterns; withdraws DD-69's brainstorm-persistence half, its context-aware-advice half stands). **Decision-integrity reconciliation across 6 spots** (the reversal touched several records that had over-claimed persistence): DD-73 full record + index row; **DD-69 corrected** — the "Supersedes DD-42/DD-55 ephemeral-overlay" sentence + the "brainstorm persists" rationale + the index-row claim all rewritten to "persistence withdrawn by DD-73; advice half stands"; **DD-42 marker corrected** from "ephemerality REVERSED by DD-69 — Lilly signed off" → "REAFFIRMED by DD-73"; SPEC §9 Brainstorm note (+distill-on-close summary) + §7 F30 insight-distillation (two on-close outputs distinguished) + §8 range→DD-73. Resolved+deleted the F10b "2 taste calls pending sign-off" item (both decided: (a) clause→chat entry IN; (b) ephemeral+summary). Distillation build queued to DEV_TODO; the as-built persists brainstorm, so this is a real rework (flagged), and the "seed not idempotent" bug likely moots with it. **NB Kevin worked in parallel this session** — DD-72 (drift-marker = `last_export_at` timestamp, the engineering ADR for DD-71) is his; I built on DD-72 as the next number (DD-73) and left it intact. **Hygiene: clean** — no record now claims brainstorm persists (all 6 reconciled); DD-73 refs resolve (DD-42/DD-55/DD-69, F10b/F11/F30, `negotiation_patterns`, `issues`); grounding-guard invariant restated consistently (ledger/clause only, never transcript/summary); privacy clean — genericized the two persona/owner names I touched in DD-69's rationale (Lilly→product owner, Katrina→product) while correcting it. **Structural: skeleton dumped (§1–17, unchanged) + touched sections read end-to-end** — DD-73 appended at the DESIGN_DECISIONS tail after Kevin's DD-72, index row in sequence; DD-69/DD-42 edits in place (no reorder); §9 + §7 line-level only. §9 remains THE standing split candidate (propose-first, awaiting Lilly's OK since 2026-06-23) — not re-litigated. Both verdicts present → counter reset to 0.
Prior: 2026-06-25 (12th — export/mark-as-sent decoupling: DD-71 recorded (donna.ai can't send → Export becomes a pure download; a separate **Mark as sent → Counterparty/Legal** action, in cockpit + My Contracts list, is the boundary event that cuts the snapshot of current state + advances the pointer + flips the badge + mints vN; non-blocking drift warning if edited since last export). **Amends DD-61.** SPEC reconciled across: §8 range→DD-71; §1 core-workflow Path A/B diagrams (Mark-as-sent cuts the snapshot, export then send manually); §5 F14 (cut on Mark-as-sent, trigger relocates), F15b (now pure download, send-branch + `{recipient}` to be removed — "built (rework pending DD-71)"), F15 (baseline gate → first Mark-as-sent), F27 (Mark-as-sent boundary action, cockpit+list, drift warning); §6 `contract_snapshots` (cut on Mark-as-sent not export); §9 export-flow diagram + F15b para + new **Mark as sent** subsection + global-nav note; DESIGN_DECISIONS DD-71 full record + index row + DD-61 forward-amendment marker. Resolved+deleted the open "[product — F15b/F15, DD-61] Send lost its home" item (its content promoted into DD-71). Engineering rework (relocate snapshot trigger, remove send-branch, build Mark-as-sent + last-export drift comparison) queued to DEV_TODO. **Hygiene: clean** — DD-71 refs resolve (DD-09/DD-22/DD-48/DD-61, F14/F15/F15b/F27/F31, `last_shared_with_X`/`contract_snapshots`/`node_versions`); DD-61 carries a forward-amendment note (no stale "send cuts snapshot" left authoritative — data-model §6, F14, §1 diagrams, §9 all reconciled to Mark-as-sent); Mode B §11 step-1 "most recent snapshot **marked as sent**" already aligns (left as-is); the two DD-70/DD-71 divergence signals (persistent "edited since last send" vs mark-time "edited since last export") distinguished in DD-71 to prevent conflation; privacy clean (generic counterparty/legal/operator, illustrative dates, no names). **Structural: skeleton dumped (§1–17, unchanged) + touched sections read end-to-end** — DD-71 appended at the DESIGN_DECISIONS tail in number order, index row in sequence; the new §9 "Mark as sent" subsection slotted directly after the F15b clean-copy para (its natural sibling); no new top-level section, no reorder. §9 (export-flow block grew ~10 lines) remains THE standing split candidate, already a propose-first item awaiting Lilly's OK (raised 2026-06-23) — not re-litigated here. Both verdicts present → counter reset to 0.
Prior: 2026-06-25 (11th — versioning-UX session: DD-70 recorded (monotonic v-numbers over snapshots; working copy never numbered/locked; passive "edited since last send" marker; badge tracks last boundary event, not local edits). SPEC reconciled: §8 DD-range→DD-70; §5 F27 row rewrite (lineage numbering + unsent-edits marker + post-send-edit badge clarification); §9 home-card badge line (+marker). DESIGN_DECISIONS: DD-70 full record + index row, **+ fixed a pre-existing gap — DD-69 had a full record but no index row and §8 said "…DD-68"; added the DD-69 index row and bumped the range to DD-70**). **Hygiene: clean** — DD-70 refs resolve (DD-22/DD-48/DD-61, F14/F21/F27, `node_versions`/`last_shared_with_counterparty`/`contract_snapshots`); DD-70 supersedes nothing but clarifies F27's badge ambiguity (post-send local edits keep `Sent`, don't flip to `In flight`) and affirms DD-48/DD-61 immutability as load-bearing; the operator's "lock/unlock + dual tags" framing captured as rejected-alternative (a), not silently dropped; privacy clean (generic counterparty/legal/operator + illustrative Jun-12 dates, no names). **Structural: skeleton dumped (§1–17, unchanged) + touched sections read end-to-end** — DD-70 appended at the DESIGN_DECISIONS tail in number order, index row in sequence; no new section, no reorder; durable home for the decision is DD-70 + the F27 row (§9 carries only the one-line badge/marker note, kept light). §9 remains THE standing split candidate, already a propose-first item awaiting Lilly's OK (raised 2026-06-23) — not re-litigated here. Both verdicts present → counter reset to 0.
Prior: 2026-06-25 (10th — public-release session: DD-63 (delete preserves deal-shared data), DD-64 (Mode B matching — LLM judges identity, deterministic redline delta), DD-65 (binary open/closed status + segmented toggle), DD-66 (Current Clause inspector tab + issue-card→clause nav), DD-67 (editable issue description + comments feature removed) recorded; §8 DD-range→DD-67; full SPEC reconciliation to DD-65/66/67 [§3/§4/§5 F07·F09·F12·F20·F31/§6 status-enum+issue_comments+comment_embeddings removed+ER diagram/§7 tiers re-based/§9 toggle+Current-Clause+editable-desc/§12/§13]). **Hygiene: clean** — zero dangling refs to issue_comments / comment_embeddings / the 5-value status taxonomy / the "New Issue" tab (all removals carry a DD-67 marker); negotiation-history capability conserved but re-based onto the issue position/decision ledger + node_versions; DD-15 principal-response channel flagged as needing a non-comment home (v1.1 portal, F22); privacy clean (generic throughout; persona + legal-firm names scrubbed repo-wide this session). **Structural: skeleton dumped (§1–17, all present + ordered) + read end-to-end** — binary open/closed consistent across §5/§6/§9/§12; Current Clause inspector documented in cockpit build-status + issue-detail; one pre-existing §9 "top tabs vs as-built right rail" divergence noted, left for a later §9 pass (out of scope here). Both verdicts present → counter reset to 0.
Prior: 2026-06-24 (9th — triggered after the Donna Q&A v1 finalization, 6 edits: §5 F10 row rewrite [single-contract, read-and-explain, cited, F05b-no-embeddings, DD-40 window, LATER boundary]; §9 Layer-3 Donna table row + new "### Donna tab — single-contract Q&A (F10)" subsection; §8 DD-range→DD-62; §7 F10 line reconciled deal-scoped→contract-scoped + whole-deal deferred; + DESIGN_DECISIONS DD-62 index row & full record). **Hygiene: clean** — all refs resolve (F05/F05b/F10/F11/F12/F28/DD-14/DD-40/DD-62, §2.4/§3/§7/§9); DD-62 supersedes nothing but **pins the four LATER boundaries** (F11/F28/F12/whole-deal) so none is silently assumed; reconciled the pre-existing §7 "over a contract or the whole deal" F10 description to the locked single-contract scope (removed a stale conflict + dropped a "Client A" example in the process — privacy net-positive); generic throughout (no names, Us/Them/operator). **Structural: skeleton dumped (§1–17, unchanged) + touched sections read end-to-end** — the Donna-tab subsection slotted into §9 directly before "Issue detail view" (parallel altitude to it); DD-62 appended at the DESIGN_DECISIONS tail in number order; no reorder. §9 (UI) remains THE standing split candidate, already a propose-first item awaiting Lilly's OK (raised 2026-06-23) — this addition reinforces it, not re-litigated here. Both verdicts present → counter reset to 0.
Prior: 2026-06-24 (8th — triggered at N=12 after the three export-thrust refinements for F15: §9 clean-copy para [send-vs-grab], §9 redline "requires a baseline" hint, §9 issue-list `#` table cell, §9 "Snapshots & baselines" rewrite [verbatim DD-61 rule], §12 step-1 snapshot, §5 F14/F15b/F15/F31 rows, §6 contract_snapshots + snapshot_pointers entities, §8 DD-range→DD-61; + DESIGN_DECISIONS DD-22 supersession note, DD-48 internal-sets-none, DD-60 `#`-sequence + DD-61 cross-ref, new DD-61 full record + index row). **Hygiene: clean** — all refs resolve (DD-09/DD-13/DD-44/DD-48/DD-60/DD-61, F14/F15/F15b/F27/F31, §2.4/§9/§12); DD-61 supersedes the DD-22 "internal exports cut a snapshot" clause + the prior §9 "Internal/Copy-only cuts a snapshot, advances no pointer" wording — both edited in place so no stale conflict remains; the §9-vs-DD-48 latent inconsistency on whether Internal advances a pointer is now resolved (Internal = pure download, no pointer; `snapshot_pointers.party='internal'` flagged dormant, not dropped); issue-list `#` decision lives once in DD-60 (DD-61 index row deliberately omits it). Privacy clean (generic Us/Them, "3.4.12", no names). **Structural: skeleton dumped (§1–17, unchanged) + touched sections read end-to-end** — every edit slotted into existing §5/§6/§8/§9/§12 homes; DD-61 appended at the DESIGN_DECISIONS tail in number order; no new section, no reorder. §9 (UI, export-flow para grew ~6 lines) remains THE standing split candidate, already a propose-first item awaiting Lilly's OK (raised 2026-06-23) — not re-litigated here. Both verdicts present → counter reset to 0.
Prior: 2026-06-24 (7th — triggered at N=6 after the export-surface spec-out: §9 Export flow rewrite [3 v1 exports, no-snapshot redline UX, DD-48 baseline, issue-list table], §12 Export Pipeline rewrite [F15b/F15/F31 split], §5 F14 clarify + F15 rewrite + new F15b + new F31 rows, §13 Phase-3 deliverable, §8 DD-range→DD-60; + DESIGN_DECISIONS DD-60 index row & full record). **Hygiene: clean** — all new refs resolve (F14/F15/F15b/F31, DD-09/DD-13/DD-43/DD-44/DD-48/DD-51/DD-60, OQ-08, §9/§12); **fixed a pre-existing stale ref** (old F15 note cited §11 for the export pipeline → §12); reconciled the pre-DD-48 single "sent to counterparty" tag wording in §9 to the DD-48 recipient-selector + named-pointer model (DD-22 already marked "generalised by DD-48", DD-60 reinforces — no new conflict); dropped non-derivable "Both" from issue-list Raised-by (single-valued `initiator` → Us/Them/—); privacy clean (generic 3.4.12 / Us-Them / "last shared with counterparty" only). **Structural: skeleton dumped (§1–17, unchanged) + the touched sections read end-to-end** — every edit slotted into existing §5/§9/§12/§13 homes; DD-60 appended at the DESIGN_DECISIONS tail in number order; no new section, no reorder. §9 (UI, now ~360 lines after the Export-flow expansion) remains THE standing split candidate, but that split is already a propose-first item awaiting Lilly's OK (raised 2026-06-23) — not re-litigated here. Both verdicts present → counter reset to 0.
Prior: 2026-06-24 (6th — triggered at N=5 after the DD-59 single-box issue-capture reconciliation: §8 DD-range bump; §9 +Issue rewrite; §9 detail-view Issue-Description note; §9 cockpit build-status line; F06 registry row [+ DD-59 index row & record in DESIGN_DECISIONS]). **Hygiene: clean** — DD-59 resolves across §8 index range (now DD-01…DD-59), the DD index table, and the full record; §9 +Issue / detail-note / build-status / F06 now all agree on the single-box model (box→`our_position`/`their_position` routed by `initiator`; `title` auto-derived); the prior "single note routed by initiator" §9 claim is reconciled with the as-built (box→title) via DD-59's named build-delta; refs DD-59/DD-50/DD-17/F06/F28 resolve; no privacy leak (generic "storage rate" / Us-Counterparty examples). **Structural: skeleton dumped (§1–17, unchanged) + the touched sections read end-to-end** — all DD-59 edits slotted into existing §9 + DD-log homes, no new section, no reorder; §9 (UI) remains THE standing split candidate but that split is already a propose-first item awaiting Lilly's OK (raised 2026-06-23), not re-litigated here. Both verdicts present → counter reset to 0.
Prior: 2026-06-24 (5th — triggered at N=6 after that session's 5 edits: F05b clause-search row; cockpit build-status clause-search mention; §9 +Issue note-routing-by-initiator; §9 Contracts no-committed-filter decision; §9 Step 1 deal-position-required-inline). **Hygiene: clean** — F05b refs (F05/F12/F20/DD-35/§2.4) resolve; §9 edits consistent with §6 (issues `our_position`/`their_position`; `deals.position` enum matches Step 1 list; F28/DD-50 source-stance aligned); no new privacy leak (generic examples only). **Structural: skeleton dumped (§1–17, unchanged count) + read end-to-end** — today's 4 additions slotted into existing §5/§9 homes; §9 (UI) remains THE split candidate and today's additions reinforce it, but the §9 split is **already a propose-first item awaiting Lilly's OK** (raised 2026-06-23) — not executed silently. No reorder needed. Both verdicts present → counter reset to 0.
Prior: Edits since last health check: 1 (2026-06-24: §6 inline-enumeration note — inline `(i)(ii)(iii)` enumerators are NOT separate clause-tree nodes in v1; revisit-triggered, gated on round-trip spike + defined-term no-split carve-out. Resolved the eng-review PM_TODO item; pruned once captured in §6.)
Latest check: 2026-06-23 (4th — prompted by Lilly "did you update/check off the TODOs?"). **Hygiene: clean** — §5 ↔ code current (Kevin's 4th reconciliation); DD index carries DD-55..58; section/DD refs resolve; **privacy now fully clean** — the genericization name-pass ran (0 residual: real name → "product owner", personas → engineering/product, "Donna" kept) and shipped in the first public push, so the prior "persona names pending" caveat is RESOLVED. **Structural: §9 split PROPOSED to Lilly** — §9 (UI) grew to ~320+ lines this session (added Navigation model + Home + Cockpit build-status + Contracts), now the clear split candidate; raised as a heads-up (propose-first refactor — NOT executed without her OK). Both verdicts present → counter reset.
Last full health check: 2026-06-22, through DD-54 + front-matter regionization (§6 `role` field, §11 step 1b) — and the session's accumulated edits (F03/F01/F02/F04 status flips, table_data, deals.position, F28/F29). Both verdicts present — Completion gate satisfied.
Hygiene: clean — section refs (none > §17) and DD refs (max DD-54, index + §8 range updated) all resolve; `role` consistent across §6 / §11 / DD-54; fixed one self-introduced privacy slip (real name → "product owner" in DD-54). Privacy still NOT fully clean: internal + persona names pending the known pre-commit genericization TASK (Open) — not a new leak.
Structural: skeleton dumped (§1–17) + read end-to-end; order sound for a new reader; today's edits slotted cleanly (role into §6 entities, step 1b into §11 Mode A, DD-54 appended, statuses into §5). No refactor needed. §9 UI (~246 lines) remains the future-split candidate (not urgent). Backup SPEC.md.bak (delete once confirmed).

## F11 — Donna recommendations: scope proposal (awaiting Lilly's sign-off)

**Status:** PROPOSAL ONLY. SPEC §5/§9 untouched until Lilly signs off the open questions below. This is the next thrust — Donna's *advisory* layer. F10 (read-and-explain Q&A) shipped; F11 is where the advice lives.

**The pain it removes.** The operator opens an issue mid-call ("storage rate too low, push to market"). Today Donna can *explain* the clause (F10) but the actual work — deciding what to counter and writing the words — is still hers, from a blank page, on the clock. F11 hands her a grounded landing + drafted clause text she can read aloud or paste. That is the effort sink, and it is the headline differentiator vs. Word. Scoped *inside* an issue (clause anchor + position ledger + audit trail), which is exactly what makes advice safe under §2.4 — the F10 box keeps deflecting; F11 is the single sanctioned advice surface.

---

**1. What Donna recommends in v1 — one recommendation object, not three features.**

v1 produces a single grounded recommendation per opened issue = **{short cited rationale + recommended landing (`recommended_position`) + drafted clause language (`donna_counter_language`)}**. Framed on DD-14 rule 4's reasonableness spectrum (favorable-but-fair → aggressive → deal-breaking) with a fallback ladder (ask / settle / floor) — the field already anticipates this and it is what makes the card briefable to a principal.

- **Propose vs. counter is the same engine with a different input field, not two builds** (collapse-before-build). Operator-raised issue → `our_position` populated → Donna recommends a landing + drafts *our* clause language. Counterparty-raised / Mode-B issue → `their_position` populated → Donna recommends a *counter* + drafts counter-language. The engine reads {clause text + our/their positions + status} and emits {landing + language + rationale}; which fields are populated decides the stance. One pass.
- **Why both reasoning AND language, as a unit:** a landing with no drafted clause leaves the hardest part (the words) undone → fails effort-saved; drafted words with no rationale are an assertion she can't defend to her principal → fails §2.4. They ship together or not at all. (The conservative cut — reasoning-only first — is **Open Q1**, a real risk call for Lilly.)
- Grounded in: clause text via the **F05b conceptual lookup (no embeddings, same as F10)**, DD-31-resolved references (defined terms / cross-refs / deal params) *before* reasoning, the issue's our/their positions + agreed/open ledger. Cited exactly like F10.

**2. Surface — auto-on-open, lazy, cached.**

- **From the operator's POV: automatic.** She opens the issue, the recommendation is in the DONNA block (already mocked in §9, lines ~655–672) below ISSUE DESCRIPTION, above the action bar. No "Ask Donna" click.
- **From the system's POV: lazy.** Generate on **first detail-view open**, not on issue creation — creating a batch of issues fast mid-call must not burn an LLM call on each one (half get deferred/dismissed; and `our_position` is likelier complete by open-time). Async loading card (reuse F10's staged-loading), persist, show cached on re-open, **[↻ Refresh]** to regenerate after she edits the description or the clause changes. *(Reconciliation flag: §5 F11 says "on open"; §9 says "after creation" — adopt "on first open" and fix §9 on sign-off.)*
- **Accept/edit/ignore:** **[Use Donna's language]** → applies `donna_counter_language` to the clause body via the standard apply path (node version + audit). **[Edit Donna's language]** → inline editor pre-filled. **Ignore** = she does nothing; the card persists as advisory (no new status, no "dismiss" — a recommendation is advice on an issue, not an issue).
- **Closes a loop:** confirming a recommendation is what finally *populates* `recommended_position` / `donna_counter_language` — the columns F31 issue-list export already renders as "Proposed resolution." F11 fills what F31 reads.

**3. Live research (DD-38) — OUT of v1.**

Deferred, by decision. v1 grounds **only** in {clause text + DD-31-resolved refs + our/their positions + agreed/open ledger}. No web. Justification: DD-49 already states the F29 static knowledge layer is meant to turn *most* "issue needs a market number" calls into local lookups — cheaper, deterministic, which §2.4 wants — and DD-38 adds a new external dependency (search API + caching + citation storage) plus the highest-correctness-risk surface (a wrong market figure screen-shared to a counterparty is the exact §2.4 credibility failure). Sequence: ship the **recommendation engine** first (F11 v1) → layer F29 static benchmarks behind it → add DD-38 live research as the top-up for figures not in F29, all without changing the operator-facing surface (the F05b→F12 pattern). *(Flag: §9's current mock shows a live "market range" block — that is the **target** F11, not v1; mark it Phase-2/DD-38 on sign-off.)*

**4. Guardrails — advises, never authoritative.**

- **Never auto-applied.** Nothing touches the clause body without an explicit [Use Donna's language] click.
- **Never auto-exported, and this needs a draft/confirmed distinction — flagging a real gap.** F31 already reads `recommended_position` / `donna_counter_language` as "Proposed resolution." If F11 auto-writes those on open, an *unconfirmed* Donna suggestion could flow into a principal-/counterparty-facing export — the precise thing DD-50/F28 forbid ("visibly marked, operator-confirmed, never cross into an export"). **Product rule: an auto-generated recommendation is a marked draft; it only populates the exported fields on operator confirmation.** The *mechanism* (a `recommendation_status` flag vs. a separate `donna_draft_*` staging field vs. reusing the auto_flag-style marking) is **Kevin's ADR — queued to him below.**
- **Relationship to F10:** the read-and-explain Q&A box still deflects "should I accept?" → "open an issue, Donna recommends there." F11 is the deflection target. Advice only ever lives inside an issue; the free-floating box never advises. That boundary is what keeps §2.4 intact.

**5. Grounding + citations.**

- Cite the clause node(s) (clickable chips, reuse F05/F05b + F10's citation rendering) + any ledger issue referenced.
- **Honest when it can't ground (DD-14 rules 2–3):** if the recommendation needs a market figure (no F29, no live research in v1) → Donna recommends the *structure* of the counter and explicitly flags "this needs a market benchmark — I can't source one from the contract," rather than inventing a number. If it needs enforceability/legal judgment → "get a lawyer" + framed question + attached clauses (reuse the legal-review-packet path). Never fabricates a figure or a legal conclusion.

**6. v1 in / out boundary.**

**MUST (v1):**
- Auto-on-open grounded recommendation: rationale + recommended landing + drafted clause language, on the reasonableness spectrum with a fallback ladder.
- Grounding: clause text (F05b lookup, DD-31 refs resolved) + our/their positions + agreed/open ledger.
- Cited to clickable clause nodes; honest-failure when it can't ground (market figure → flag; legal → "get a lawyer").
- Renders in the issue-detail DONNA block; [Use Donna's language] / [Edit] / ignore / [Refresh].
- Writes `recommended_position` / `donna_counter_language` **only on operator confirm** (draft until then; not exported unconfirmed).

**LATER (named, deferred by decision — not silently assumed):**
- Live web research for market figures → **DD-38** (after F29).
- Static knowledge-layer grounding (benchmarks / risk taxonomy) → **F29** (Phase 2).
- Proactive issue flagging (Donna raising issues herself) → **F28** (Phase 2+). F11 only recommends on issues that *already exist*.
- Implicit semantic retrieval (embeddings) → **F12** (Phase 2). v1 uses the F05b lookup, same as F10.
- Negotiation-history RAG (prior rounds / contested-node history) → **F13** (Phase 2). v1 grounds in the *current* ledger only.
- The interactive **"Brainstorm with Donna" overlay** (DD-42, the ephemeral back-and-forth + DD-55 extraction) → **slice 2** (it's a chat surface = real build). v1 ships the one-shot card only. (This is **Open Q2**.)

**7. Open questions for Lilly (taste / priority — only she can call these).**

- **Q1 — Drafted language in v1, or reasoning-only first?** My rec: **both in** — drafting is the effort win (DD-27: "the operator's job is to judge Donna's language, not write from scratch"). The conservative cut ships rationale + recommended landing only (lower hallucination surface — no generated clause text), with drafted language as slice 2. Genuine risk call: how far do you trust Donna to draft clause words you'll paste mid-call vs. writing them yourself from her reasoning?
- **Q2 — One-shot recommendation card only, or also the interactive Brainstorm overlay in F11 v1?** My rec: **one-shot only**; Brainstorm overlay (DD-42 + DD-55 extraction) is slice 2 — it's a whole chat surface. Ship the static card first, or bundle the brainstorm now?
- **Q3 — When a recommendation needs a market figure Donna can't ground (no F29 / no live research): partial-with-a-flag, or nothing?** My rec: **recommend the structure of the counter and flag the missing benchmark** ("needs a market number — can't source from the contract") rather than suppressing the whole card — a flagged-but-useful recommendation beats a blank. Alternative: show nothing for market-figure issues until F29/DD-38, on the view that a half-recommendation is worse than none. Which do you want for the gap case?
- **Q4 (lighter) — Auto-on-open vs. explicit "Ask Donna" trigger?** My rec: **auto-on-open** (effort-saved; opening an issue is a clear intent signal, unlike F28). Raise only because cost-control / not-feeling-pushed is a legit preference — say if you'd rather she click to summon it.

**Queued to Kevin (engineering, on sign-off — not blocking the proposal):** schema mechanism for the draft-vs-confirmed recommendation state (§4 guardrail) so unconfirmed auto-recommendations never reach the F31 export — `recommendation_status` flag vs. `donna_draft_*` staging field vs. auto_flag-style marking. His ADR.

---

## Open

- [ ] **[?] [F32 firm profile — toggle granularity, not blocking] Fixed/Donna-editable: one global toggle or per-section?** Spec'd as one global toggle (DD-80) per Lilly's phrasing. Open: should the operator be able to lock *some* sections (e.g. "core commercial interests" Fixed) while letting Donna evolve others? **My rec: global v1, per-section as a later refinement** — global is the simplest mental model and matches how she described it; revisit only if she finds herself wanting to pin part of the profile. Perceivable, so it's her call when F32 is scheduled.
- [ ] **[?] [F30 trigger — needs Lilly's sign-off, blocks the build] Re-anchor distillation from "brainstorm-session close" → "issue close."** F30 (DD-55) + the DD-73 per-issue summary both fire "on brainstorm close," but **no brainstorm-session entity/close event exists** — brainstorm is just the persistent per-contract Donna thread (`donna_conversations`), unbounded, never closed. **My rec: fire both on-close distillations on the EXISTING issue-close event (F07 Open→Closed, stamps `resolved_at`), distilling from the committed issue ledger + decision + clause text — never the raw transcript.** Dissolves the missing prerequisite (no brainstorm lifecycle to build), is grounding-safe by construction, and de-couples F30 from the still-unbuilt DD-73 ephemeral rework. Perceivable behavior change (timing + captured content) → her call, not Kevin's ADR. If she insists on literal brainstorm-close, the prerequisite becomes real machinery (brainstorm-session entity + lifecycle + DD-73 ephemeral rework) and F30 is genuinely gated. Captured in the scope memo to Kevin; promote to DD-76 + amend DD-55/DD-73 on her OK.
- [ ] **[product — F03e semantics] Trailing sentence after the last enumerated item: parent (lead-in) vs last child.** Spike GREENLIT (12/12 byte-identical round-trip). One genuine product call it left open: in `Intro: (a) X; (b) Y. This applies only in writing.` does the trailing sentence belong to the **parent (lead-in)** node or stay glued to the **last child (b)**? The spike currently glues it to (b) — byte-safe either way, so this is a semantics/addressability call (mine), not a round-trip one.
  **My rec: it belongs to the PARENT (the list frame), NOT child (b) — discriminated by sentence boundary.** A trailing run that forms its **own sentence** after the last item (terminal punctuation closing (b), then a new capitalized sentence — Lilly's example) is a **list-wide qualifier**: it scopes the whole enumeration, is the same cognitive unit as the lead-in, and the parent frames the list top **and** bottom. Gluing a list-scoped qualifier inside (b) **misattributes** it — open an issue on (b) and you drag in an unrelated scoping sentence; redline (b) and the scope is wrong. F03e's whole point is per-item addressability; burying a list qualifier in (b) defeats it.
  **Honest carve-out (prevents the opposite error):** where the post-marker text is a **grammatical continuation of the last item with no sentence break** (e.g. `(b) Y, except as set out in Section 4`), it **stays part of (b)** — the item's own tail, not a list qualifier. Discriminator is the **sentence boundary**, not raw position. New-sentence case → parent; continuation case → last child.
  **Eng implication (Kevin's schema DD owns the mechanism — not my call):** attributing the trailing sentence to the list frame means the parent must carry a **post-children trailing segment** (same explicit-separator discipline as the `separator_after` item below), OR a dedicated **trailing sibling node** at the parent's level — never glue to (b). I'm fixing the product invariant (trailing sentence ≠ child (b)); Kevin picks the storage shape.
- [ ] **[for Kevin — F03e, agreed schema condition feeding the eventual schema DD] Explicit separator storage.** The spike found a **trimmed child body cannot round-trip**, so the build MUST store the inter-item separator explicitly — Kevin recommends a **nullable `separator_after`** column on the child node. Logged here as the agreed condition that feeds Kevin's schema DD (engineering owns the mechanism; recorded so it isn't lost to chat). Ties to the trailing-segment storage in the F03e-semantics item above.
- [ ] **[for Kevin — F03e inline enumerator splitting, greenlit 2026-06-25] Spike FIRST, then build.** Lilly reversed the prior won't-fix-v1: inline `(a)(b)(c)` / `(i)(ii)(iii)` runs inside one paragraph must now split into ordered children of their lead-in clause (lead-in text = parent body; each marker = ordered child; order preserved). Spec lives in §6 (inline-enumeration note) + §5 F03e. **Hard sequence: run the round-trip-oracle de-risk spike BEFORE touching the parser** — one paragraph → N nodes → reassemble → assert **byte-identical to source**, with the **defined-term carve-out** (`"Term" means (i)… (ii)…` never split) as an explicit no-split case in the spike. This is the parked engineering gate from the original defer; it still binds. Build constraints to honour: **flat-only in v1** (don't recurse nested `(a)…(i)…(b)`); **keep the literal `(a)`/`(i)` marker in the child body** (do NOT strip + re-derive — Donna doesn't reliably regenerate alpha/roman, and the literal marker is what makes the round-trip byte-identical); **new imports only** (no retroactive re-split of committed trees, no migration). Engineering ADR in DESIGN_DECISIONS if the spike surfaces a reassembly nuance; queue to DEV_TODO once the spike passes. Flip F03e §5 status when shipped.
- [ ] **[for Kevin — F31 issue-list export, Phase 3] Field-source mapping for the .docx render.** `#` ← **1..n sequence assigned in render order** (sort `issues.priority` DESC, NULLs last, tie-break document order, free-floating last — then number rows 1..n; do **not** print the raw `priority` value, it's an internal triage number, not counterparty-safe; the printed `#` is a stable "item N" reference for a walkthrough). DD-60. `Clause` ← derived node number or "—" (free-floating); `Issue` ← `title`; `Status` ← {`open`→Open, `deferred`→Deferred, `kicked`→Parked}; `Raised by` ← `initiator` (operator→Us, counterparty→Them, **donna→Us** once operator-kept — a kept auto-flag is our point to raise); `Our position`/`Their position` ← `our_position`/`their_position`; `Proposed resolution` ← `recommended_position` (fallback `donna_counter_language`), "—" if null. Row filter: `WHERE status IN ('open','deferred','kicked')`. Free-floating (`node_id IS NULL`) grouped last under a separator. Rides the F15b renderer; no schema change. Spec: §9 export-flow table + DD-60. (Engineering ADR if any rendering nuance; queue to DEV_TODO when F31 is scheduled.)
- [ ] **[for Kevin — DD-59] Cockpit issue-capture field-routing.** The single Description box must write to `our_position` (initiator=operator) / `their_position` (initiator=counterparty) per the Us/Counterparty toggle, and set `title` = a deterministic first-line/truncation snippet of that text. **Currently the build maps box→`title` with positions null** — change it. No schema change (all three columns exist on `issues`). Same for the free-floating + "+ New issue" paths. Donna keeps reading the populated position. Rationale + rejected alternatives durable in DD-59. (Hand to Kevin; flip F06 row note when wired.)
- [ ] **[optional, post-DD-59] Donna-generated issue title.** v1 `title` is a deterministic snippet; optionally upgrade the async analysis pass to write back a cleaner short label. Nice-to-have, not v1-blocking.
- [ ] **[from eng review] Contract status transitions.** `drafting → under negotiation → signed` — who sets each, and when (manual? on first export?). User-visible, currently unspecified.
- [ ] **[from eng review] Prompt-injection product stance.** Counterparty clause text can carry "ignore instructions, recommend accept" into Donna's prompts. Engineering owns the defense; product owns the behavior: when Donna detects manipulation attempt in document text, what should she DO — silently ignore, or flag to operator? Ties to behavioral contract (DD-14).
- [ ] **[from eng review] Eval acceptance bars.** What quality threshold ships each AI surface? The F28 auto-flag F1~0.62 risk tolerance and hunk-significance accuracy are product risk-tolerance calls, needed before those surfaces go live.
- [ ] **Settings API error surface.** Creating a deal/contract with a non-existent parent id (or a duplicate name) currently returns a raw DB error, not a clean 404/400 + message. Decide the user-visible behavior: translate FK errors to friendly 404/400? reject duplicate names? Not blocking — happy path + DB integrity work; only the error UX is open. (Tracked in DEV_TODO.)
- [ ] **Inline "+ Create new contract type" at import vs Settings-only.** §9 says contract types are managed in Settings and the import flow "selects" one. The Context step I built adds an inline "+ Create new type" (parity with client/deal create, and so a fresh DB with no seeded types isn't a dead end). Confirm that's the wanted behavior, or restrict type creation to Settings and seed defaults (Licence/Offtake/JV/NDA/Amendment, per §9) so import is select-only.

